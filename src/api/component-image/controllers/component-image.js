'use strict';

/**
 * component-image controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::component-image.component-image');
